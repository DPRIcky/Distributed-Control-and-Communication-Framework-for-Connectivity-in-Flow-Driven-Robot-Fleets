[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "holoocean-pruning-project"
version = "0.1.0"
description = "HoloOcean + Distributed Pruning integration"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
include = ["pruning*"]
